<div class="container d-flex align-items-center justify-content-center min-vh-100">   <%# container:Bootstrapのコンテナ（中央寄せ、余白調整） d-flex, align-items-center, justify-content-center: Flexboxで縦横中央寄せ%>
  <div class="card shadow p-4 login-card"> <%# card:Bootstrapのカードコンポーネント login-card:独自スタイル%>
    <div class="text-center mb-4">
      <h2 class="text-pink">ログイン</h2>
      <p>よりそいノートへ入る</p>
    </div>

    <%= form_with model: resource, url: session_path(resource_name), scope:resource_name, local: true, data: { turbo: false } do |f| %>    <%# Deviseログイン用フォーム resourceやresource_nameはDeviseが自動で用意する変数（例：User） %>
      <div class="mb-3"> <%# labelはフォームラベル email_field:メール入力欄%>
        <%= f.label :email, 'メールアドレス', class: 'form-label' %>
        <%= f.email_field :email, autofocus: true, autocomplete: 'email', class: 'form-control', required: true %>
      </div>
      <div class="mb-3">
        <%= f.label :password, 'パスワード', class: 'form-label' %>
        <%= f.password_field :password, autocomplete: "current-password", class: 'form-control', required: true %>
      </div>

      <div class="d-grid">
        <%= f.submit "ログインする", class: "btn btn-pink-override btn-lg" %>
      </div>
    <% end %>
      <hr class="my-4">

      <div class="text-center">
        <%= link_to "パスワードを忘れた方はこちら", new_user_password_path, class: "text-secondary d-block mb-2" %>
        <%= link_to "新規登録はこちら", new_registration_path(resource_name), class: "btn btn-outline-secondary w-100" %>
  </div>
</div>
